use_default_settings: true

general:
  debug: false
  instance_name: harness-searxng

server:
  secret_key: "ultrasecretkey"  # overridden at runtime by SEARXNG_SECRET
  limiter: false
  image_proxy: false

search:
  safe_search: 0
  autocomplete: ""
  formats:
    - html
    - json

ui:
  static_use_hash: true
