# Tasks

- [x] 1. Add focused tests for installed and absent OpenSpec CLI initialization paths.
- [x] 2. Add fail-fast, idempotent telemetry opt-out to the cross-platform init entry point.
- [x] 3. Document the global setting, optional-tool behavior, and re-enable command.
- [x] 4. Run focused tests, strict OpenSpec validation, and refresh the Wiki index.
- [x] 5. Review all pending files and run `python harness.py check` before archiving.
- [x] 6. Prepare the verified telemetry change for repository integration by the enclosing task.
