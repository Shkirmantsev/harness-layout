# OpenSpec rules

OpenSpec owns normative behavioral intent/change; `.ai/wiki/` explains the current system.

- `specs/` = current agreed behavior.
- `changes/<id>/` = proposed/future behavior until verified/archived.
- Do not invent requirements from implementation alone.
- Report spec/implementation disagreement explicitly.
- Every non-trivial change should identify its knowledge/context impact.
