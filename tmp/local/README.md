# Local generated state

Everything below `tmp/local/` is local, reproducible, disposable runtime/cache/index state and should be ignored by Git except this README.

The project-context index is created under:

`tmp/local/project-context/`
